import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    black: "#333333",
    white: "#e6e4de",
    blue: "#1d2a58",
    red: "#672719",
  },
});

export { theme };
